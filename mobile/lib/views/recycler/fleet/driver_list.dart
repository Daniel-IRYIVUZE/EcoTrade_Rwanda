import 'package:flutter/material.dart';
class DriverListWidget extends StatelessWidget {
  const DriverListWidget({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Driver list view'));
}
