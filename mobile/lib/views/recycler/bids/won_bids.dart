import 'package:flutter/material.dart';
class WonBids extends StatelessWidget {
  const WonBids({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Won bids view'));
}
