import 'package:flutter/material.dart';
class ActiveBids extends StatelessWidget {
  const ActiveBids({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Active bids view'));
}
