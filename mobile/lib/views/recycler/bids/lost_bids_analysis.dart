import 'package:flutter/material.dart';
class LostBidsAnalysis extends StatelessWidget {
  const LostBidsAnalysis({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Lost bids analysis'));
}
